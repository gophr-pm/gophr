apiVersion: v1
kind: ReplicationController
metadata:
  name: indexer-rc
  labels:
    module: indexer
  namespace: gophr
spec:
  replicas: 1
  selector:
    module: indexer
  template:
    metadata:
      labels:
        module: indexer
      namespace: gophr
    spec:
      containers:
      - name: indexer
        image: gcr.io/valid-song-142700/gophr-indexer:1.0.2
        volumeMounts:
        - mountPath: /secrets
          name: secrets
      volumes:
        - name: secrets
          secret:
            secretName: gophr-secrets
