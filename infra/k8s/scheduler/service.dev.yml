apiVersion: v1
kind: Service
metadata:
  name: scheduler-svc
  labels:
    module: scheduler
  namespace: gophr
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      nodePort: 30100
  selector:
    module: scheduler
