apiVersion: v1
kind: ReplicationController
metadata:
  name: depot-int-rc
  labels:
    module: depot-int
  namespace: gophr
spec:
  replicas: 1
  selector:
    module: depot-int
  template:
    metadata:
      labels:
        module: depot-int
      namespace: gophr
    spec:
      containers:
      - name: depot-int
        image: gcr.io/valid-song-142700/gophr-depot-int:1.0.0
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /secrets
          name: secrets
        - mountPath: /repos
          name: repos
      volumes:
      - name: repos
        gcePersistentDisk:
          pdName: gophr-volume-depot
          fsType: int4
      - name: secrets
        secret:
          secretName: gophr-secrets
