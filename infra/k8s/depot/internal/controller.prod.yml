apiVersion: v1
kind: ReplicationController
metadata:
  name: depot-int-rc
  labels:
    module: depot-int
  namespace: gophr
spec:
  replicas: 1
  selector:
    module: depot-int
  template:
    metadata:
      labels:
        module: depot-int
      namespace: gophr
    spec:
      containers:
      - name: depot-int
        image: gcr.io/valid-song-142700/gophr-depot-int:1.0.3
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /repos
          name: repos
      volumes:
      - name: repos
        gcePersistentDisk:
          pdName: gophr-volume-depot
          fsType: ext4
          readOnly: false
