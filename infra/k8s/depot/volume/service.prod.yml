apiVersion: v1
kind: Service
metadata:
  name: depot-vol-svc
  labels:
    module: depot-vol
  namespace: gophr
spec:
  ports:
    - name: nfs
      port: 2049
    - name: rpcbind
      port: 111
  securityContext:
  selector:
    module: depot-vol
